<!DOCTYPE html>
<html>
  <head>
    <title>Signup</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

       <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">




    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap JS (Optional) -->
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


  </head>
 <body class="text-info bg-success">


          <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">SESSIONS</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            
         




      <ul class="navbar-nav">
        

        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link  nav-colour" %>
        </li>


        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link nav-colour" %>
        </li>

         <% if current_user  %>
        <li class="nav-item">
             <%= link_to 'Profile', profile_path, class: "nav-link nav-colour" %>
        </li>


        <li class="nav-item">
          <li><%= link_to 'Posts', posts_path, class: "nav-link nav-colour" %>
          <%# <a class="nav-link disabled" aria-disabled="true">Disabled</a> %>
        </li>

         <nav-colourli class="nav-item">
             <%= link_to "Logout", logout_path, data: { "turbo-method": :delete }, class: "nav-link nav-colour" %>
        </li>
                 <% else %>

           <li class="nav-item">
             <%= link_to "Login", login_path, class: "nav-link nav-colour" %>
           </li>

            <li class="nav-item">
             <%= link_to "Signup", signup_path, class: "nav-link nav-colour" %>
             
           </li>

            
           <% end %>
      </ul>
    </div>
  </div>
</nav>
    </header>

   <main>
     <%= yield %>

       <% flash.each do |key, value| %>
       <div class="flash <%= key %>"><%= value %></div>
       <% end %>
       <%# <%= yield %>
    </main>
  </body>
</html>